<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yyjy.web.mapper.ArticleMapper">

    <resultMap id="ArticleCardMap" type="com.yyjy.web.domain.vo.response.ArticleCardRes">
        <id property="id" column="id"/>
        <result property="title" column="title"/>
        <result property="summary" column="summary"/>
        <result property="thumbnail" column="thumbnail"/>
        <result property="createTime" column="create_time"/>
        <result property="viewCount" column="view_count"/>
        <result property="likeCount" column="like_count"/>
        <result property="commentCount" column="comment_count"/>
        <collection property="tags" ofType="java.lang.String">
            <result column="tag_name"/>
        </collection>
    </resultMap>

    <select id="page" resultMap="ArticleCardMap">
        select
            a.id as id,
            a.title as title,
            a.summary as summary,
            a.thumbnail as thumbnail,
            a.create_time as create_time,
            a.view_count as view_count,
            (select count(*) from article_like as al where al.article_id = a.id) as like_count,
            (select count(*) from comment where comment.article_id = a.id) as comment_count,
            t.name as tag_name
        from article as a
            left join category as c on a.category_id = c.id
            left join article_tag as at on a.id = at.article_id
            left join tag as t on at.tag_id = t.id
        <where>
            a.status = 0 and a.del_flag = 0
            <if test="req.categoryId != null">
                and a.category_id = #{req.categoryId}
            </if>
        </where>
        order by a.is_top desc, a.create_time desc

    </select>
    <select id="getArticleById" resultType="com.yyjy.web.domain.vo.response.ArticleRes"
            parameterType="java.lang.Long">
        select
            a.id as id,
            a.title as title,
            a.content as content,
            a.create_time as create_time,
            a.view_count as view_count,
            (select count(*) from article_like as al where al.article_id = a.id) as like_count,
            (select count(*) from comment where comment.article_id = a.id) as comment_count
        from article as a
                 left join category as c on a.category_id = c.id
        <where>
            a.status = 0 and a.del_flag = 0 and a.id = #{id}
        </where>
    </select>
</mapper>
