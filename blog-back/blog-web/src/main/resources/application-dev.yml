spring:
  application:
    name: blog-admin
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://47.111.151.165:3306/blog?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=UTC
    username: root
    password: ma7p28itxK38s4RB

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.yyjy.model.entity
  configuration:
    map-underscore-to-camel-case: true
  #    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

  global-config:
    db-config:
      logic-delete-field: delFlag  # 逻辑删除字段名（数据库中的列名）
      logic-delete-value: 1        # 逻辑已删除时字段的值
      logic-not-delete-value: 0    # 逻辑未删除时字段的值
      id-type: auto
      update-strategy: not_null
logging:
  level:
    comment:
      wall:
        mapper: debug
        service: info
        controller: info

springdoc:
  # 默认是false，需要设置为true
  default-flat-param-object: true

minio:
  endpoint: http://47.111.151.165:9000
  access-key: 1Fl2gM0wwlvoWnXeHD94
  secret-key: JtwECGOR5F71Bn3GiBh5s9gOlcXco299tYiMl3BU
  bucket: blog
